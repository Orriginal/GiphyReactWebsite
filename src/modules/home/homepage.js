import React from 'react';
import GiphyGridContainer from '../giphy-grid/giphy-grid-container';

export default function Homepage(props) {
  console.group('Michael');
  console.group('is');
  console.group('een');
  console.group('noob');
  console.groupEnd();
  console.groupEnd();
  console.groupEnd();
  console.groupEnd();
  return (
    <main>
      <GiphyGridContainer />
    </main>
  );
}
